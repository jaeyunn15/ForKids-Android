<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nh.hackaton.forkids">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ForkidsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ForKids"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.VideoActivity"></activity>
        <activity android:name=".ui.activity.CustomButtonActivity"></activity>
        <activity android:name=".ui.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.SignUpActivity"></activity>
        <activity android:name=".ui.activity.MainActivity" />
    </application>

</manifest>